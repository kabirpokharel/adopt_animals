import React, { useState } from "react";

const localCache = {};

const useBreedList = (animal) => {
  const [breedList, setBreedList] = useState([]);
  return <div>useBreedList</div>;
};

export default useBreedList;
